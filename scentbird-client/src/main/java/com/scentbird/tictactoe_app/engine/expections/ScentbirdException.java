package com.scentbird.tictactoe_app.engine.expections;


// It doesn't do anything, but we could use it instead of runtime to throw our specific exeptions to handle them and so on
public class ScentbirdException extends RuntimeException {

}
